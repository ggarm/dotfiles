#!/bin/sh
# Update a file with the number of updates

FILE='/home/ricardo/Scripts/pacman_updates'

#pacman -Syy >/dev/null #must be done as root

NUM=`pacman -Qu | wc -l`

if [[ $NUM -gt 0 ]]; then
	echo $NUM > $FILE
else
	echo '' > $FILE
fi
